/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/6.2/userguide/multi_project_builds.html
 */ 
 
	
dependencyResolutionManagement {
    repositories {
        mavenLocal()
        maven {
            url = uri("https://maven.pkg.github.com/ofirghc/_")
            credentials {
                username = System.getenv("Blah")
                password = System.getenv("bcvs")
            }
        }
    }
}
	
pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = "GitHubPackages"
			url = "https://maven.pkg.github.com/ofirghc/_"
			credentials {
					username = System.getenv("Blah")
					password = System.getenv("Blah")
				}
		}
		mavenLocal()	
        }
	
	
	
	//blah 
	
    }

rootProject.name = 'client-repo'
